# Plant-Leaf-Disease-Detection-using-CNN
Plant Leaf Disease Detection using CNN is a deep learning project that classifies plant leaf images as healthy or diseased. The dataset is divided into training, validation, and testing sets. Using image preprocessing and augmentation, the CNN model learns leaf patterns and achieves about 91% training accuracy and 83% validation accuracy.
